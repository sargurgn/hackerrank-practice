/*
var is hoisted in global.
let and const are hoisted in temperol dead zone.

Accessing var before declaration will result in undefined.
Accessing let and const before declaration will give an error - variable is not defined.
*/
